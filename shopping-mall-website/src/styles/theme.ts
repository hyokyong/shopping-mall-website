import { DefaultTheme } from 'styled-components';
import { colors, font, media, variable, animation } from './const';

const theme: DefaultTheme = {
  media,
  colors,
  variable,
  font,
  animation,
};

export { theme };
